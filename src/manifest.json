{
  "name": "Bytes per Tab",
  "version": "1.0",
  "description": "Shows each tab's network usage (if any) in their title bar",
  "permissions": [
    "experimental", "tabs", "<all_urls>"
  ],
  "background": {
    "scripts": ["bytes.js"]
  },
  "manifest_version": 2
}
